# Redundant Screens (Archived)

These files are archived redundant implementations and are **not** part of the active runtime path.

Archived from:

- `src/features/home/HomeScreen.tsx`
- `src/features/optimize/OptimizeScreen.tsx`
- `src/features/transactions/TransactionsScreen.tsx`

Current active path is:

- `src/features/new/*` -> `src/features/legacy/*`
